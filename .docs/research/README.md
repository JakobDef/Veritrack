# Research

Findings produced by the `researcher` agent: codebase deep dives and external API/library investigations. Filename format: `YYYY-MM-DD-<short-slug>.md`. Each note states the Question, a short answer, evidence with file paths or URLs, and open questions. Notes are synthesized, not raw search dumps.
