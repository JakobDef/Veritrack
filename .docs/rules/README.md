# Rules

Hard, non-negotiable rules that are too granular for `AGENTS.md`. One concept per file, short and imperative, named `<short-name>.md`. The session-start hook injects every file in this directory into every session, so each rule is a permanent context cost: add one only when it is genuinely non-negotiable. Rules are promoted from learnings or added by the user.
